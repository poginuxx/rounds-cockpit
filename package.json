{
  "name": "rounds-cockpit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A personal neurology rounding cockpit — local-first, encrypted, installable PWA.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "vendor:ocr": "node scripts/vendor-tesseract.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "tesseract.js": "^7.0.0"
  }
}
